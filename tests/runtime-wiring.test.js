'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),path=require('path');
const root=path.join(__dirname,'..');
const {createCompositionRoot}=require('../dist/app/composition-root');
const fakePool={query:async()=>({rows:[],rowCount:0}),connect:async()=>({query:async()=>({rows:[],rowCount:0}),release(){}})};
const express={raw:()=>((_req,_res,next)=>next&&next())};
const composition=createCompositionRoot({pool:fakePool,rootDir:root,appVersion:'8.0.0',express});
const ctx=composition.services;
const expected=["htmlEsc", "chromiumBin", "salePdfHtml", "cookie", "sha256", "randomToken", "serverId", "nextDocumentNumber", "scryptHash", "verifyHash", "safeUser", "hasPerm", "hasAction", "needAction", "canReadStore", "canWriteStore", "sanitizeRecord", "rowValue", "redactRecordForUser", "branchOf", "consumeReceiptProvenance", "restoreReceiptProvenance", "normalizeSaleCatalog", "clinicalAlertsForProducts", "prepareSaleCommercial", "prepareInventoryCommercial", "prepareInsuranceClaimsCommercial", "prepareShiftCommercial", "prepareExpenseCommercial", "preparePurchaseCommercial", "prepareSupplierReturnCommercial", "prepareReturnCommercial", "nowIso", "originOkay", "rateLimit", "firstTenant", "standaloneSetupAllowed", "ownerSetupMeta", "normalizeProviderBrand", "ownerProviderBrand", "ownerManagedClaims", "dropSessionCache", "loadSession", "authOptional", "needAuth", "needPerm", "bumpChange", "auditDb", "checkLicenseLimit", "base32Decode", "base32Encode", "encryptSecret", "decryptSecret", "verifyTotp", "issueSession", "ensureLicenseWritable", "licenseFeature", "initDb", "verifyCurrentCredential", "getRecord", "revisionConflict", "ensureCoreAccounts", "validateJournalRecord", "putRecord", "delRecord", "querySearchFields", "safeSortExpr", "buildFinancialReconciliation", "periodLocked", "applySalePutBatch", "applyAtomicOp", "accountingRows", "currentDeviceContext", "currentOpenShiftForPayment", "validateOperationalCashMoveShifts", "paymentAccount", "backupKey", "makeBackup", "compareVersions", "claimIntegrationRows", "integrationWorker", "catalogFeedSync", "licenseWorker", "autoUpdateWorker"];
test('composition root composes every commercial helper',()=>{for(const n of expected)assert.equal(typeof ctx[n],'function',n);assert.equal(ctx.sha256('abc').length,64);assert.match(ctx.salePdfHtml({lines:[],total:0},{},{enabled:false}),/<!doctype html>/)});
test('all API routes register using scoped dependencies',()=>{const calls=[];const app={};for(const m of ['get','post','put','patch','delete'])app[m]=(...args)=>calls.push([m,args[0]]);composition.registerRoutes(app);assert.equal(calls.length,80);assert.equal(new Set(calls.map(x=>x.join(' '))).size,80)});
