import{AppError}from'../../../core/errors/app-error.js';import{newId}from'../../../core/types/id.js';import type{DbTx}from'../../../core/db/types.js';import type{AuditContract}from'../../audit/contracts/audit-contract.js';import type{InventoryContract}from'../../inventory/contracts/inventory-contract.js';import type{TrackTraceContract,TraceEventType,TrackTraceRecorder}from'../contracts/tracktrace-contract.js';import type{SettingsContract}from'../../settings/contracts/settings-contract.js';
export class TrackTraceService implements TrackTraceRecorder{constructor(private readonly repo:TrackTraceContract,private readonly inventory:InventoryContract,private readonly audit:AuditContract,private readonly settings?:SettingsContract){}async manual(t:string,u:string,i:{branchId:string;productId:string;batchId?:string|null;gtin?:string|null;serialNumber?:string|null;eventType:TraceEventType;quantity:number;sourceType?:string;sourceId?:string|null;note?:string|null}){const prefs=this.settings?await this.settings.get(t):null;if(prefs?.preferences.trackTraceEnabled===false)throw new AppError('TRACE_DISABLED','التتبع الدوائي غير مفعّل من الإعدادات',409);if(prefs?.preferences.traceRequireBatchNo&&!i.batchId)throw new AppError('TRACE_BATCH_REQUIRED','الإعدادات تشترط اختيار تشغيلة لحدث التتبع',422);let batchNo:null|string=null;if(i.batchId){const b=await this.inventory.getBatch(t,i.batchId);if(!b)throw new AppError('TRACE_BATCH_NOT_FOUND','التشغيلة غير موجودة',404);if(b.productId!==i.productId||b.branchId!==i.branchId)throw new AppError('TRACE_BATCH_MISMATCH','التشغيلة لا تطابق الصنف أو الفرع',409);batchNo=b.batchNo;}if(!(i.quantity>0))throw new AppError('TRACE_QTY_INVALID','كمية التتبع يجب أن تكون أكبر من صفر',422);const x=await this.repo.record({id:newId('trc'),tenantId:t,branchId:i.branchId,productId:i.productId,batchId:i.batchId??null,batchNo,gtin:i.gtin?.trim()||null,serialNumber:i.serialNumber?.trim()||null,eventType:i.eventType,quantity:i.quantity,sourceType:i.sourceType?.trim()||'manual',sourceId:i.sourceId?.trim()||null,note:i.note?.trim()||null,userId:u,createdAt:new Date().toISOString()});await this.audit.record({tenantId:t,userId:u,action:`trace.${i.eventType}`,entity:'trace_event',entityId:x.id,detail:{productId:i.productId,batchId:i.batchId,sourceId:i.sourceId}});return x;}async recordDispense(t:string,u:string,i:{branchId:string;productId:string;batchId:string;batchNo?:string|null;quantity:number;saleId:string},tx:DbTx){if(await this.repo.existsSource(t,'dispensed','sale',i.saleId,i.batchId,tx))return;await this.repo.record({id:newId('trc'),tenantId:t,branchId:i.branchId,productId:i.productId,batchId:i.batchId,batchNo:i.batchNo??null,gtin:null,serialNumber:null,eventType:'dispensed',quantity:i.quantity,sourceType:'sale',sourceId:i.saleId,note:'auto from POS',userId:u,createdAt:new Date().toISOString()},tx);}}
