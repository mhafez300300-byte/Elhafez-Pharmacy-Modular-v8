import test from'node:test';import assert from'node:assert/strict';import{CashService}from'../src/modules/cash/application/cash-service.js';
const uow:any={withTransaction:async(fn:any)=>fn({})};
test('cash shift open and close are audited in the same transaction',async()=>{const calls:any[]=[];const cash:any={openShift:async(i:any)=>({...i,openedAt:'x',closedAt:null,closingCash:null,expectedCash:null,variance:null,status:'open'}),getShift:async()=>({id:'s',tenantId:'t',branchId:'b',userId:'u',openedAt:'x',closedAt:null,openingCash:100,closingCash:null,expectedCash:null,variance:null,status:'open'}),closeShift:async()=>({id:'s',tenantId:'t',branchId:'b',userId:'u',openedAt:'x',closedAt:'y',openingCash:100,closingCash:110,expectedCash:105,variance:5,status:'closed'})};const audit:any={record:async(x:any)=>calls.push(x)};const svc=new CashService(uow,cash,audit);await svc.open('t','u',{branchId:'b',openingCash:100});await svc.close('t','u',{shiftId:'s',closingCash:110});assert.deepEqual(calls.map(x=>x.action),['shift.opened','shift.closed']);assert.equal(calls[1].detail.variance,5);});
