import { Router } from 'express';
import { requireAuth } from '../../../core/http/require-auth.js';
import { requirePermission } from '../../../core/http/require-permission.js';
import { asyncHandler } from '../../../core/http/async-handler.js';
export const settlementRoutes = (s, rp) => { const r = Router(); r.use(requireAuth); r.get('/obligations', requirePermission('settlements.read'), asyncHandler(async (req, res) => res.json({ items: await rp.listObligations(req.auth.tenantId, req.query.partyType ? String(req.query.partyType) : undefined, req.query.partyId ? String(req.query.partyId) : undefined) }))); r.get('/credits', requirePermission('settlements.read'), asyncHandler(async (req, res) => res.json({ items: await rp.listCredits(req.auth.tenantId, req.query.partyType ? String(req.query.partyType) : undefined, req.query.partyId ? String(req.query.partyId) : undefined) }))); r.get('/payments', requirePermission('settlements.read'), asyncHandler(async (req, res) => res.json({ items: await rp.listPayments(req.auth.tenantId, req.query.partyType ? String(req.query.partyType) : undefined, req.query.partyId ? String(req.query.partyId) : undefined) }))); r.get('/credit-balance/:partyType/:partyId', requirePermission('settlements.read'), asyncHandler(async (req, res) => res.json({ balance: await rp.creditBalance(req.auth.tenantId, String(req.params.partyType), String(req.params.partyId)) }))); r.get('/balance/:partyType/:partyId', requirePermission('settlements.read'), asyncHandler(async (req, res) => res.json({ balance: await rp.balance(req.auth.tenantId, req.params.partyType, req.params.partyId) }))); r.post('/payments', requirePermission('settlements.pay'), asyncHandler(async (req, res) => res.status(201).json(await s.pay(req.auth.tenantId, req.auth.userId, { branchId: String(req.body?.branchId ?? ''), partyType: req.body?.partyType, partyId: String(req.body?.partyId ?? ''), method: req.body?.method ?? 'cash', amount: Number(req.body?.amount ?? 0), ...(req.body?.reference ? { reference: String(req.body.reference) } : {}) })))); return r; };
