export const customerMigrations = [{ id: '005_customers', sql: `CREATE TABLE crm_customers(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,phone text NULL,credit_limit numeric(14,2) NOT NULL DEFAULT 0 CHECK(credit_limit>=0),active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX crm_customers_tenant_idx ON crm_customers(tenant_id,active);` }];
