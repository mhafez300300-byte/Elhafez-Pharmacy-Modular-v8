export const cashMigrations = [{ id: '010_cash', sql: `CREATE TABLE cash_shifts(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),user_id text NOT NULL REFERENCES id_users(id),opening_cash numeric(14,2) NOT NULL DEFAULT 0,closing_cash numeric(14,2) NULL,status text NOT NULL CHECK(status IN('open','closed')),opened_at timestamptz NOT NULL DEFAULT now(),closed_at timestamptz NULL);CREATE UNIQUE INDEX cash_one_open_shift_per_branch ON cash_shifts(tenant_id,branch_id) WHERE status='open';CREATE TABLE cash_movements(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),shift_id text NOT NULL REFERENCES cash_shifts(id),kind text NOT NULL,amount numeric(14,2) NOT NULL,reference_type text NOT NULL,reference_id text NOT NULL,note text NULL,created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX cash_movements_shift_idx ON cash_movements(tenant_id,shift_id,created_at);` }, { id: '022_cash_reconciliation', sql: `ALTER TABLE cash_shifts ADD COLUMN IF NOT EXISTS expected_cash numeric(14,2) NULL;ALTER TABLE cash_shifts ADD COLUMN IF NOT EXISTS variance numeric(14,2) NULL;` },
    { id: '035_cash_perf', sql: `CREATE INDEX cash_shifts_branch_time_idx ON cash_shifts(tenant_id,branch_id,opened_at DESC);CREATE INDEX cash_movements_branch_time_idx ON cash_movements(tenant_id,branch_id,created_at DESC);` }
];
