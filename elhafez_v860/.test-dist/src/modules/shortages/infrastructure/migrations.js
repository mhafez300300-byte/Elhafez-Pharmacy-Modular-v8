export const shortageMigrations = [{ id: '039_shortage_notebook', sql: `CREATE TABLE IF NOT EXISTS ops_shortages(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),product_id text NULL REFERENCES cat_products(id),free_text text NULL,quantity numeric(14,3) NOT NULL DEFAULT 1 CHECK(quantity>0),customer_id text NULL REFERENCES crm_customers(id),customer_name text NULL,customer_phone text NULL,reason text NULL,note text NULL,status text NOT NULL DEFAULT 'open' CHECK(status IN('open','ordered','fulfilled','cancelled')),source text NOT NULL DEFAULT 'manual',created_by text NOT NULL REFERENCES id_users(id),created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),CHECK(product_id IS NOT NULL OR length(trim(COALESCE(free_text,'')))>0));CREATE INDEX IF NOT EXISTS ops_shortages_tenant_branch_status_idx ON ops_shortages(tenant_id,branch_id,status,created_at DESC);CREATE INDEX IF NOT EXISTS ops_shortages_product_idx ON ops_shortages(tenant_id,product_id) WHERE product_id IS NOT NULL;` }];
