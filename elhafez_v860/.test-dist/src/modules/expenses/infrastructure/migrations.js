export const expenseMigrations = [{ id: '016_expenses', sql: `CREATE TABLE exp_expenses(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),user_id text NOT NULL REFERENCES id_users(id),category text NOT NULL,description text NOT NULL,amount numeric(14,2) NOT NULL CHECK(amount>0),method text NOT NULL CHECK(method IN('cash','card','bank')),occurred_at timestamptz NOT NULL DEFAULT now());CREATE INDEX exp_expenses_tenant_time_idx ON exp_expenses(tenant_id,occurred_at DESC);` }];
