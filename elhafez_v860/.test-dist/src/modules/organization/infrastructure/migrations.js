export const organizationMigrations = [{ id: '001_organization', sql: `CREATE TABLE org_tenants(id text PRIMARY KEY,name text NOT NULL,status text NOT NULL CHECK(status IN('trial','active','suspended','expired')),currency char(3) NOT NULL DEFAULT 'EGP',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE TABLE org_branches(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX org_branches_tenant_idx ON org_branches(tenant_id,active);` }];
