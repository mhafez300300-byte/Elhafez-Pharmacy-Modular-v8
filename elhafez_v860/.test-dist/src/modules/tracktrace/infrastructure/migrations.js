export const trackTraceMigrations = [{ id: '042_track_trace', sql: `CREATE TABLE IF NOT EXISTS trace_events(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),product_id text NOT NULL REFERENCES cat_products(id),batch_id text NULL REFERENCES inv_batches(id),batch_no text NULL,gtin text NULL,serial_number text NULL,event_type text NOT NULL CHECK(event_type IN('received','dispensed','returned','quarantined','released','recalled','adjusted')),quantity numeric(14,3) NOT NULL CHECK(quantity>0),source_type text NOT NULL,source_id text NULL,note text NULL,user_id text NULL REFERENCES id_users(id),created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS trace_events_batch_idx ON trace_events(tenant_id,batch_id,created_at DESC);CREATE INDEX IF NOT EXISTS trace_events_lookup_idx ON trace_events(tenant_id,gtin,serial_number,batch_no,created_at DESC);CREATE UNIQUE INDEX IF NOT EXISTS trace_events_source_once_idx ON trace_events(tenant_id,event_type,source_type,source_id,batch_id) WHERE source_id IS NOT NULL;` }];
