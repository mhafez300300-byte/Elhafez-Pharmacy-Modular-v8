export const supplierMigrations = [{ id: '006_suppliers', sql: `CREATE TABLE crm_suppliers(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,phone text NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX crm_suppliers_tenant_idx ON crm_suppliers(tenant_id,active);` }];
