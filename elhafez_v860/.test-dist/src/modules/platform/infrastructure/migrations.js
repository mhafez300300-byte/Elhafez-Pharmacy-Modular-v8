export const platformMigrations = [{ id: '008_platform', sql: `CREATE TABLE platform_activation(tenant_id text PRIMARY KEY REFERENCES org_tenants(id) ON DELETE CASCADE,company_code text NULL,mode text NOT NULL CHECK(mode IN('owner_center','standalone')),status text NOT NULL,plan text NOT NULL,expires_at timestamptz NULL,features jsonb NOT NULL DEFAULT '[]'::jsonb,updated_at timestamptz NOT NULL DEFAULT now());` }];
