export const clinicalMigrations = [{ id: '019_clinical', sql: `CREATE TABLE ph_doctors(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,phone text NULL,specialty text NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now());CREATE TABLE ph_prescriptions(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,customer_id text NULL REFERENCES crm_customers(id),doctor_id text NULL REFERENCES ph_doctors(id),code text NOT NULL,issued_at timestamptz NOT NULL DEFAULT now(),expires_at timestamptz NULL,status text NOT NULL CHECK(status IN('active','dispensed','cancelled')),UNIQUE(tenant_id,code));CREATE TABLE ph_prescription_lines(id text PRIMARY KEY,prescription_id text NOT NULL REFERENCES ph_prescriptions(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES cat_products(id),quantity numeric(14,3) NOT NULL CHECK(quantity>0),dose text NULL);CREATE INDEX ph_prescriptions_tenant_idx ON ph_prescriptions(tenant_id,issued_at DESC);CREATE TABLE ph_recalls(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES cat_products(id),batch_no text NULL,reason text NOT NULL,severity text NOT NULL CHECK(severity IN('warn','block')),active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX ph_recalls_active_idx ON ph_recalls(tenant_id,product_id,active);CREATE TABLE ph_interactions(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,product_a text NOT NULL REFERENCES cat_products(id),product_b text NOT NULL REFERENCES cat_products(id),severity text NOT NULL CHECK(severity IN('warn','block')),message text NOT NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),CHECK(product_a<>product_b));CREATE INDEX ph_interactions_idx ON ph_interactions(tenant_id,product_a,product_b,active);` }];
