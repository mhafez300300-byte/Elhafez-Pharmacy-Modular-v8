export const settingsMigrations = [{ id: '007_settings', sql: `CREATE TABLE sys_settings(tenant_id text PRIMARY KEY REFERENCES org_tenants(id) ON DELETE CASCADE,pharmacy_name text NOT NULL,phone text NULL,address text NULL,invoice_footer text NULL,updated_at timestamptz NOT NULL DEFAULT now());` }, { id: '038_settings_preferences', sql: `ALTER TABLE sys_settings ADD COLUMN IF NOT EXISTS email text NULL;ALTER TABLE sys_settings ADD COLUMN IF NOT EXISTS whatsapp text NULL;ALTER TABLE sys_settings ADD COLUMN IF NOT EXISTS tax_number text NULL;ALTER TABLE sys_settings ADD COLUMN IF NOT EXISTS commercial_registration text NULL;ALTER TABLE sys_settings ADD COLUMN IF NOT EXISTS preferences jsonb NOT NULL DEFAULT '{}'::jsonb;` }];
