import{api}from'../api/client.js';import{h}from'../components/dom.js';import{state}from'../state/store.js';
export async function automationPage(){const[s,p]=await Promise.all([api<any>(`/api/automation/snapshot?branchId=${encodeURIComponent(state.branch?.id??'')}`),api<any>('/api/settings')]);const pref=p?.preferences??{},root=h('div'),head=h('div',{class:'section-head'},h('div',{},h('h2',{},'مركز الأتمتة'),h('p',{},'السياسات التي ينفذها النظام تلقائيًا وما يحتاج تدخلًا بشريًا'))),grid=h('div',{class:'grid kpis'},k('اقتراحات شراء',s.recommendedPurchaseLines),k('حرج للشراء',s.criticalPurchaseLines),k('تتبع البيع الآلي',s.traceSalesEnabled?'مفعّل':'متوقف'),k('الملخص اليومي',s.dailyBriefEnabled?'مفعّل':'متوقف')),cards=h('div',{class:'settings-action-grid'});cards.append(action('إعادة الطلب الذكية',s.reorderEnabled,'يحسب الاستهلاك والتغطية ويقترح الكمية','replenishment'),action('تتبع التشغيلات آليًا',pref.trackTraceAutoSales!==false,'يسجل التشغيلات المصروفة من FEFO داخل فاتورة البيع','track_trace'),action('كشكول النواقص من POS',s.shortageCaptureEnabled,'يسجل طلب العميل عندما لا يجد الصنف','shortages'),action('تنبيهات الصلاحية',pref.notifyExpiry!==false,`قبل ${pref.expiryWarningDays??90} يومًا حسب سياستك`,'expiry'),action('مطالبات التأمين',pref.insuranceEnabled!==false,'إنشاء ومتابعة المطالبات المرتبطة بفواتير البيع','insurance'));root.append(head,grid,h('section',{class:'card'},h('div',{class:'card-title'},h('h3',{},'سياسات تعمل بدل المستخدم')),cards,h('div',{class:'notice success'},'الأتمتة لا تتجاوز حدود الدومينات: البيع يظل مسؤولًا عن الفاتورة، المخزون عن FEFO، والتتبع يسجل أثر التشغيلات عبر Contract مستقل.')));return root;}
function k(l:string,v:any){return h('div',{class:'card kpi'},h('small',{},l),h('strong',{},String(v)))}function action(t:string,on:boolean,sub:string,page:string){return h('button',{class:'action-card',onClick:()=>window.dispatchEvent(new CustomEvent('elhafez:navigate',{detail:page}))},h('b',{},`${on?'●':'○'} ${t}`),h('small',{},sub));}
