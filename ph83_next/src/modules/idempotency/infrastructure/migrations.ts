import type { Migration } from '../../../core/db/migrator.js';
export const idempotencyMigrations:readonly Migration[]=[{id:'029_idempotency',sql:`CREATE TABLE idem_keys(tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,scope text NOT NULL,key text NOT NULL,request_hash text NOT NULL,status text NOT NULL CHECK(status IN('processing','completed')),resource_id text NULL,created_at timestamptz NOT NULL DEFAULT now(),completed_at timestamptz NULL,PRIMARY KEY(tenant_id,scope,key));CREATE INDEX idem_keys_created_idx ON idem_keys(created_at);`}];
