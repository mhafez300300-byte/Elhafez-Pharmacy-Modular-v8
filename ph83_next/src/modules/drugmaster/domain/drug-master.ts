import{AppError}from'../../../core/errors/app-error.js';
export type DrugMasterInput=Readonly<{gtin?:string|null;barcode?:string|null;nameAr:string;nameEn?:string|null;activeIngredients?:readonly string[];strength?:string|null;dosageForm?:string|null;manufacturer?:string|null;requiresPrescription?:boolean;controlledClass?:string|null;officialPrice?:number|null;source?:string|null;sourceUpdatedAt?:string|null}>;
export function normalizeDrugMaster(input:DrugMasterInput){const nameAr=String(input.nameAr??'').trim();if(nameAr.length<2)throw new AppError('DRUG_MASTER_NAME_REQUIRED','اسم الدواء المرجعي مطلوب',422);const clean=(v:unknown)=>String(v??'').trim()||null;const officialPrice=input.officialPrice==null?null:Number(input.officialPrice);if(officialPrice!=null&&(!Number.isFinite(officialPrice)||officialPrice<0))throw new AppError('DRUG_MASTER_PRICE_INVALID','السعر الرسمي غير صحيح',422);const activeIngredients=[...new Set((input.activeIngredients??[]).map(x=>String(x).trim()).filter(Boolean))];return{gtin:clean(input.gtin),barcode:clean(input.barcode),nameAr,nameEn:clean(input.nameEn),activeIngredients,strength:clean(input.strength),dosageForm:clean(input.dosageForm),manufacturer:clean(input.manufacturer),requiresPrescription:input.requiresPrescription===true,controlledClass:clean(input.controlledClass),officialPrice,source:clean(input.source)??'manual',sourceUpdatedAt:clean(input.sourceUpdatedAt)};}
export function parseActiveIngredients(value:string){return[...new Set(String(value??'').split(/[+;,|]/).map(x=>x.trim()).filter(Boolean))];}
export function parseBoolean(value:unknown){const x=String(value??'').trim().toLowerCase();return['1','true','yes','y','rx','prescription','مطلوب','نعم'].includes(x);}
