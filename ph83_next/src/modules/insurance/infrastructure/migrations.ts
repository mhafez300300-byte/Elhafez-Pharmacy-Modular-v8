import type{Migration}from'../../../core/db/migrator.js';
export const insuranceMigrations:readonly Migration[]=[{id:'041_insurance_contracts_claims',sql:`
CREATE TABLE IF NOT EXISTS ins_providers(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,code text NULL,phone text NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,name));
CREATE TABLE IF NOT EXISTS ins_plans(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,provider_id text NOT NULL REFERENCES ins_providers(id) ON DELETE CASCADE,name text NOT NULL,coverage_percent numeric(5,2) NOT NULL DEFAULT 80 CHECK(coverage_percent BETWEEN 0 AND 100),copay_percent numeric(5,2) NOT NULL DEFAULT 20 CHECK(copay_percent BETWEEN 0 AND 100),annual_limit numeric(14,2) NULL CHECK(annual_limit IS NULL OR annual_limit>=0),active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,provider_id,name));
CREATE TABLE IF NOT EXISTS ins_claims(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,sale_id text NOT NULL REFERENCES sales_invoices(id),customer_id text NULL REFERENCES crm_customers(id),provider_id text NOT NULL REFERENCES ins_providers(id),plan_id text NULL REFERENCES ins_plans(id),policy_number text NULL,patient_name text NULL,gross_amount numeric(14,2) NOT NULL CHECK(gross_amount>=0),covered_amount numeric(14,2) NOT NULL CHECK(covered_amount>=0),patient_amount numeric(14,2) NOT NULL CHECK(patient_amount>=0),status text NOT NULL DEFAULT 'draft' CHECK(status IN('draft','submitted','approved','rejected','settled')),reference_no text NULL,note text NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,sale_id));
CREATE INDEX IF NOT EXISTS ins_claims_tenant_status_idx ON ins_claims(tenant_id,status,created_at DESC);` }];
