import { readdir, readFile } from 'node:fs/promises';import path from'node:path';
const roots=['src','web'];const bannedNames=['app-shell.js','server-bridge.js','pharma-db.js','shared-finance.js','commercial-features.js','commercial-pages.js','sales-intelligence.js','operations-control.js','list-explorer.js'];const bannedText=[/v7\.\d+/i,/PharmaApp/,/settings_main/,/compatibilitydata/i];const violations=[];
async function walk(dir){for(const e of await readdir(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())await walk(p);else if(e.isFile()){const base=path.basename(p).toLowerCase();if(bannedNames.includes(base))violations.push(`${p}: legacy filename`);if(/\.(ts|html|css)$/.test(base)){const text=await readFile(p,'utf8');for(const rule of bannedText)if(rule.test(text))violations.push(`${p}: legacy marker ${rule}`);}}}}
for(const r of roots)await walk(r);if(violations.length){console.error('Clean-room violations:\n'+violations.join('\n'));process.exit(1);}console.log('Clean-room check PASS — no legacy v7 runtime markers or legacy UI filenames.');
