import{newId}from'../../../core/types/id.js';import{AppError}from'../../../core/errors/app-error.js';import type{UnitOfWork}from'../../../core/db/unit-of-work.js';import type{AuditContract}from'../../audit/contracts/audit-contract.js';import type{ShortageContract,ShortageStatus}from'../contracts/shortage-contract.js';
export class ShortageService{constructor(private readonly uow:UnitOfWork,private readonly repo:ShortageContract,private readonly audit:AuditContract){}async create(t:string,u:string,i:{branchId:string;productId?:string|null;freeText?:string|null;quantity:number;customerId?:string|null;customerName?:string|null;customerPhone?:string|null;reason?:string|null;note?:string|null;source?:string}){const free=i.freeText?.trim()||null,product=i.productId?.trim()||null;if(!i.branchId||(!product&&!free)||!Number.isFinite(i.quantity)||i.quantity<=0)throw new AppError('SHORTAGE_INVALID','بيانات كشكول النواقص غير صحيحة',422);return this.uow.withTransaction(async tx=>{const customerId=i.customerId?.trim()||null;const duplicate=await this.repo.findRecentDuplicate(t,i.branchId,product,free,customerId,10,tx);if(duplicate)throw new AppError('SHORTAGE_DUPLICATE','تم تسجيل هذا النقص بالفعل منذ وقت قصير',409,{existingId:duplicate.id});const now=new Date().toISOString(),v=await this.repo.create({id:newId('shr'),tenantId:t,branchId:i.branchId,productId:product,freeText:free,quantity:i.quantity,customerId,customerName:i.customerName?.trim()||null,customerPhone:i.customerPhone?.trim()||null,reason:i.reason?.trim()||null,note:i.note?.trim()||null,status:'open',source:i.source?.trim()||'manual',createdBy:u,createdAt:now,updatedAt:now},tx);await this.audit.record({tenantId:t,userId:u,action:'shortage.created',entity:'shortage',entityId:v.id,detail:{branchId:v.branchId,productId:v.productId,freeText:v.freeText,quantity:v.quantity}},tx);return v;});}async status(t:string,u:string,id:string,status:ShortageStatus){return this.uow.withTransaction(async tx=>{const old=await this.repo.get(t,id,tx);if(!old)throw new AppError('SHORTAGE_NOT_FOUND','سطر النواقص غير موجود',404);const v=await this.repo.updateStatus(t,id,status,tx);await this.audit.record({tenantId:t,userId:u,action:'shortage.status',entity:'shortage',entityId:id,detail:{from:old.status,to:status}},tx);return v;});}}
