import{Router}from'express';import{requirePermission}from'../../../core/http/require-permission.js';import{requireAuth}from'../../../core/http/require-auth.js';import{asyncHandler}from'../../../core/http/async-handler.js';import{newId}from'../../../core/types/id.js';import{AppError}from'../../../core/errors/app-error.js';import type{CustomerContract}from'../contracts/customer-contract.js';export const customerRoutes=(repo:CustomerContract)=>{const r=Router();r.use(requireAuth);r.get('/',requirePermission('customers.read'),asyncHandler(async(req,res)=>res.json({items:await repo.list(req.auth!.tenantId,String(req.query.q??''))})));r.post('/',requirePermission('customers.manage'),asyncHandler(async(req,res)=>{const name=String(req.body?.name??'').trim();if(name.length<2)throw new AppError('CUSTOMER_NAME_REQUIRED','اسم العميل مطلوب',422);res.status(201).json(await repo.create({id:newId('cus'),tenantId:req.auth!.tenantId,name,phone:req.body?.phone?String(req.body.phone):null,creditLimit:Number(req.body?.creditLimit??0),active:true}));}));return r;};
