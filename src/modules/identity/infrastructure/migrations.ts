import type{Migration}from'../../../core/db/migrator.js';export const identityMigrations:readonly Migration[]=[
{id:'002_identity',sql:`CREATE TABLE id_users(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,username text NOT NULL,role text NOT NULL,pin_hash text NOT NULL,permissions jsonb NOT NULL DEFAULT '[]'::jsonb,max_discount_percent numeric(5,2) NOT NULL DEFAULT 0 CHECK(max_discount_percent>=0 AND max_discount_percent<=100),active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,username));CREATE INDEX id_users_tenant_idx ON id_users(tenant_id,active);`},
{id:'028_identity_security',sql:`CREATE TABLE id_sessions(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,user_id text NOT NULL REFERENCES id_users(id) ON DELETE CASCADE,client_label text NULL,created_at timestamptz NOT NULL DEFAULT now(),last_seen_at timestamptz NOT NULL DEFAULT now(),expires_at timestamptz NOT NULL,revoked_at timestamptz NULL);CREATE INDEX id_sessions_user_active_idx ON id_sessions(tenant_id,user_id,expires_at DESC) WHERE revoked_at IS NULL;CREATE INDEX id_sessions_expiry_idx ON id_sessions(expires_at) WHERE revoked_at IS NULL;CREATE TABLE id_login_attempts(login_key text PRIMARY KEY,failures integer NOT NULL DEFAULT 0 CHECK(failures>=0),window_started_at timestamptz NOT NULL DEFAULT now(),last_failure_at timestamptz NOT NULL DEFAULT now(),blocked_until timestamptz NULL,updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX id_login_attempts_blocked_idx ON id_login_attempts(blocked_until) WHERE blocked_until IS NOT NULL;CREATE INDEX id_users_login_idx ON id_users(lower(username),active);`}
];
