import type{UnitOfWork}from'../../../core/db/unit-of-work.js';import{AppError}from'../../../core/errors/app-error.js';import{newId}from'../../../core/types/id.js';import type{OrganizationContract}from'../../organization/contracts/organization-contract.js';import type{IdentityContract}from'../../identity/contracts/identity-contract.js';import type{SettingsContract}from'../../settings/contracts/settings-contract.js';import type{AuditContract}from'../../audit/contracts/audit-contract.js';import type{ActivationContract,PlatformActivationStoreContract}from'../../platform/contracts/activation-contract.js';import type{AccountingContract}from'../../accounting/contracts/accounting-contract.js';
export class SetupService{constructor(private readonly uow:UnitOfWork,private readonly org:OrganizationContract,private readonly identity:IdentityContract,private readonly settings:SettingsContract,private readonly audit:AuditContract,private readonly activation:ActivationContract,private readonly platformStore:PlatformActivationStoreContract,private readonly accounting:AccountingContract,private readonly allowStandalone:boolean){}async status(){const q=await this.org.getTenant('default').catch(()=>null);return{configured:!!q};}
async setup(input:{pharmacyName:string;branchName:string;currency:string;adminName:string;adminUsername:string;adminPin:string;companyCode?:string}){const pharmacyName=input.pharmacyName.trim(),branchName=input.branchName.trim(),adminName=input.adminName.trim(),adminUsername=input.adminUsername.trim();if(pharmacyName.length<2||branchName.length<2)throw new AppError('SETUP_NAMES_REQUIRED','اسم الصيدلية والفرع مطلوبان',422);if(adminName.length<2||adminUsername.length<2)throw new AppError('SETUP_ADMIN_REQUIRED','بيانات المدير مطلوبة',422);if(!/^\d{4,8}$/.test(input.adminPin))throw new AppError('SETUP_PIN_INVALID','PIN المدير يجب أن يكون من 4 إلى 8 أرقام',422);let claims=null;if(input.companyCode?.trim())claims=await this.activation.resolve(input.companyCode.trim());if(!claims&&!this.allowStandalone)throw new AppError('OWNER_ACTIVATION_REQUIRED','يلزم كود شركة صالح من مركز المالك',412);if(claims&&['suspended','expired'].includes(claims.status))throw new AppError('OWNER_SUBSCRIPTION_INACTIVE','اشتراك الشركة غير نشط',403);return this.uow.withTransaction(async tx=>{const existing=await tx.query('SELECT id FROM org_tenants LIMIT 1');if(existing.rowCount)throw new AppError('SETUP_ALREADY_COMPLETED','تم إعداد النظام بالفعل',409);const tenantId='default',branchId=newId('brn'),userId=newId('usr');const name=claims?.customerName||pharmacyName;const {tenant,branch}=await this.org.createTenantWithBranch({tenantId,name,currency:input.currency||'EGP',branchId,branchName},tx);const user=await this.identity.createOwner({id:userId,tenantId,name:adminName,username:adminUsername,pin:input.adminPin},tx);await this.settings.save({tenantId,pharmacyName:name,phone:null,address:null,invoiceFooter:null},tx);await this.accounting.ensureDefaultChart(tenantId,tx);await this.platformStore.saveActivation({tenantId,companyCode:claims?.customerCode??null,mode:claims?'owner_center':'standalone',status:claims?.status??'trial',plan:claims?.plan??'trial',expiresAt:claims?.expiresAt??null,features:claims?.features??[]},tx);await this.audit.record({tenantId,userId,action:'system.setup',entity:'tenant',entityId:tenantId,detail:{branchId,mode:claims?'owner_center':'standalone'}},tx);return{tenant,branch,user,mode:claims?'owner_center':'standalone'};});}}
