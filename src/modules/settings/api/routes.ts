import{Router}from'express';import{requirePermission}from'../../../core/http/require-permission.js';import{requireAuth}from'../../../core/http/require-auth.js';import{asyncHandler}from'../../../core/http/async-handler.js';import{defaultSettingsPreferences,type SettingsContract}from'../contracts/settings-contract.js';
const bool=(v:any,d:boolean)=>v===undefined?d:v===true||v==='true';const num=(v:any,d:number,min:number,max:number)=>{const n=Number(v);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):d;};
export const settingsRoutes=(s:SettingsContract)=>{const r=Router();r.use(requireAuth);r.get('/',requirePermission('settings.read'),asyncHandler(async(req,res)=>res.json(await s.get(req.auth!.tenantId))));r.put('/',requirePermission('settings.manage'),asyncHandler(async(req,res)=>{const old=await s.get(req.auth!.tenantId),p=req.body?.preferences??{},base=old?.preferences??defaultSettingsPreferences;res.json(await s.save({tenantId:req.auth!.tenantId,pharmacyName:String(req.body?.pharmacyName??old?.pharmacyName??'').trim(),phone:req.body?.phone?String(req.body.phone):null,address:req.body?.address?String(req.body.address):null,email:req.body?.email?String(req.body.email):null,whatsapp:req.body?.whatsapp?String(req.body.whatsapp):null,taxNumber:req.body?.taxNumber?String(req.body.taxNumber):null,commercialRegistration:req.body?.commercialRegistration?String(req.body.commercialRegistration):null,invoiceFooter:req.body?.invoiceFooter?String(req.body.invoiceFooter):null,preferences:{defaultPaymentMethod:['cash','card','credit'].includes(p.defaultPaymentMethod)?p.defaultPaymentMethod:base.defaultPaymentMethod,showProfitInPos:bool(p.showProfitInPos,base.showProfitInPos),autoFocusPosSearch:bool(p.autoFocusPosSearch,base.autoFocusPosSearch),confirmBeforeSale:bool(p.confirmBeforeSale,base.confirmBeforeSale),reorderSuggestionsEnabled:bool(p.reorderSuggestionsEnabled,base.reorderSuggestionsEnabled),lowStockCoverageDays:num(p.lowStockCoverageDays,base.lowStockCoverageDays,1,120),expiryWarningDays:num(p.expiryWarningDays,base.expiryWarningDays,7,730),paperSize:p.paperSize==='80mm'?'80mm':'A4',dailyBriefEnabled:bool(p.dailyBriefEnabled,base.dailyBriefEnabled),notifyLowStock:bool(p.notifyLowStock,base.notifyLowStock),notifyExpiry:bool(p.notifyExpiry,base.notifyExpiry),compactNavigation:bool(p.compactNavigation,base.compactNavigation)}}))}));return r;};
