import type { UnitOfWork } from '../../../core/db/unit-of-work.js';import { newId } from '../../../core/types/id.js';import { AppError } from '../../../core/errors/app-error.js';import type { AuditContract } from '../../audit/contracts/audit-contract.js';import type { SalesDraftContract } from '../contracts/sales-draft-contract.js';import { normalizeSuspendedSale,type SuspendedSaleInput } from '../domain/sales-draft.js';
export class SalesDraftService{constructor(private readonly uow:UnitOfWork,private readonly repo:SalesDraftContract,private readonly audit:AuditContract){}
 async suspend(t:string,u:string,input:SuspendedSaleInput){const n=normalizeSuspendedSale(input),now=new Date().toISOString(),id=newId('drf');return this.uow.withTransaction(async tx=>{const v=await this.repo.save({id,tenantId:t,userId:u,...n,createdAt:now,updatedAt:now},tx);await this.audit.record({tenantId:t,userId:u,action:'sales.draft.suspended',entity:'sales_draft',entityId:id,detail:{label:v.label,branchId:v.branchId,lines:v.lines.length}},tx);return v;});}
 async remove(t:string,u:string,id:string){return this.uow.withTransaction(async tx=>{const before=await this.repo.get(t,id);if(!before)throw new AppError('DRAFT_NOT_FOUND','الفاتورة المعلقة غير موجودة',404);await this.repo.delete(t,id,tx);await this.audit.record({tenantId:t,userId:u,action:'sales.draft.deleted',entity:'sales_draft',entityId:id,detail:{label:before.label}},tx);return{deleted:true};});}
}
