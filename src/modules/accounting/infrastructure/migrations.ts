import type{Migration}from'../../../core/db/migrator.js';
export const accountingMigrations:readonly Migration[]=[
{id:'011_accounting',sql:`CREATE TABLE acc_journal_entries(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),reference_type text NOT NULL,reference_id text NOT NULL,description text NOT NULL,status text NOT NULL CHECK(status='posted'),created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,reference_type,reference_id));CREATE TABLE acc_journal_lines(entry_id text NOT NULL REFERENCES acc_journal_entries(id) ON DELETE CASCADE,line_no int NOT NULL,account_code text NOT NULL,debit numeric(14,2) NOT NULL DEFAULT 0 CHECK(debit>=0),credit numeric(14,2) NOT NULL DEFAULT 0 CHECK(credit>=0),memo text NULL,PRIMARY KEY(entry_id,line_no),CHECK(NOT(debit>0 AND credit>0)));CREATE INDEX acc_journal_tenant_time_idx ON acc_journal_entries(tenant_id,created_at DESC);`},
{id:'025_accounting_chart_periods',sql:`CREATE TABLE acc_accounts(tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,code text NOT NULL,name_ar text NOT NULL,type text NOT NULL CHECK(type IN('asset','liability','equity','revenue','contra_revenue','expense','cogs')),normal_side text NOT NULL CHECK(normal_side IN('debit','credit')),active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(tenant_id,code));CREATE TABLE acc_periods(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,from_date date NOT NULL,to_date date NOT NULL,status text NOT NULL CHECK(status IN('open','closed')) DEFAULT 'open',closed_by text NULL,closed_at timestamptz NULL,reopened_by text NULL,reopened_at timestamptz NULL,created_at timestamptz NOT NULL DEFAULT now(),CHECK(from_date<=to_date));CREATE INDEX acc_periods_tenant_dates_idx ON acc_periods(tenant_id,from_date,to_date);ALTER TABLE acc_journal_entries ADD COLUMN posting_date date NOT NULL DEFAULT current_date;CREATE INDEX acc_journal_posting_date_idx ON acc_journal_entries(tenant_id,posting_date);`}
];
