import type{Migration}from'../../../core/db/migrator.js';export const purchaseMigrations:readonly Migration[]=[{id:'013_purchases',sql:`CREATE TABLE purchase_counters(tenant_id text PRIMARY KEY REFERENCES org_tenants(id) ON DELETE CASCADE,last_no bigint NOT NULL);CREATE TABLE purchase_order_counters(tenant_id text PRIMARY KEY REFERENCES org_tenants(id) ON DELETE CASCADE,last_no bigint NOT NULL);CREATE TABLE purchase_orders(id text PRIMARY KEY,number text NOT NULL,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),supplier_id text NOT NULL REFERENCES crm_suppliers(id),user_id text NOT NULL REFERENCES id_users(id),status text NOT NULL CHECK(status IN('open','partial','received','cancelled')),created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,number));CREATE TABLE purchase_order_lines(id text PRIMARY KEY,order_id text NOT NULL REFERENCES purchase_orders(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES cat_products(id),ordered_qty numeric(14,3) NOT NULL CHECK(ordered_qty>0),received_qty numeric(14,3) NOT NULL DEFAULT 0 CHECK(received_qty>=0),unit_cost numeric(14,4) NOT NULL CHECK(unit_cost>=0),CHECK(received_qty<=ordered_qty));CREATE TABLE purchase_receipts(id text PRIMARY KEY,number text NOT NULL,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,branch_id text NOT NULL REFERENCES org_branches(id),supplier_id text NOT NULL REFERENCES crm_suppliers(id),user_id text NOT NULL REFERENCES id_users(id),payment text NOT NULL CHECK(payment IN('cash','credit')),total numeric(14,2) NOT NULL CHECK(total>=0),status text NOT NULL CHECK(status='received'),order_id text NULL REFERENCES purchase_orders(id),created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,number));CREATE TABLE purchase_lines(id text PRIMARY KEY,purchase_id text NOT NULL REFERENCES purchase_receipts(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES cat_products(id),quantity numeric(14,3) NOT NULL CHECK(quantity>0),unit_cost numeric(14,4) NOT NULL CHECK(unit_cost>=0),batch_no text NULL,expiry_date date NULL);CREATE INDEX purchase_receipts_tenant_time_idx ON purchase_receipts(tenant_id,created_at DESC);CREATE INDEX purchase_orders_tenant_status_idx ON purchase_orders(tenant_id,status,created_at DESC);`},{id:'023_purchase_returns',sql:`ALTER TABLE purchase_lines ADD COLUMN IF NOT EXISTS batch_id text NULL REFERENCES inv_batches(id);CREATE TABLE purchase_returns(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,purchase_id text NOT NULL REFERENCES purchase_receipts(id),branch_id text NOT NULL REFERENCES org_branches(id),supplier_id text NOT NULL REFERENCES crm_suppliers(id),user_id text NOT NULL REFERENCES id_users(id),settlement text NOT NULL CHECK(settlement IN('reduce_payable','cash_refund')),total numeric(14,2) NOT NULL CHECK(total>0),reason text NULL,created_at timestamptz NOT NULL DEFAULT now());CREATE TABLE purchase_return_lines(id text PRIMARY KEY,return_id text NOT NULL REFERENCES purchase_returns(id) ON DELETE CASCADE,purchase_line_id text NOT NULL REFERENCES purchase_lines(id),product_id text NOT NULL REFERENCES cat_products(id),batch_id text NOT NULL REFERENCES inv_batches(id),quantity numeric(14,3) NOT NULL CHECK(quantity>0),amount numeric(14,2) NOT NULL CHECK(amount>=0));CREATE INDEX purchase_returns_tenant_time_idx ON purchase_returns(tenant_id,created_at DESC);`},
{id:'034_purchase_approval',sql:`ALTER TABLE purchase_orders ADD COLUMN IF NOT EXISTS approved_at timestamptz NULL,ADD COLUMN IF NOT EXISTS approved_by text NULL REFERENCES id_users(id);`},
{id:'033_purchase_perf',sql:`CREATE INDEX purchase_receipts_supplier_time_idx ON purchase_receipts(tenant_id,supplier_id,created_at DESC);CREATE INDEX purchase_orders_supplier_status_idx ON purchase_orders(tenant_id,supplier_id,status,created_at DESC);CREATE INDEX purchase_lines_product_idx ON purchase_lines(product_id,purchase_id);`}
];
