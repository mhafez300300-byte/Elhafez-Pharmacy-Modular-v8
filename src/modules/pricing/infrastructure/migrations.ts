import type{Migration}from'../../../core/db/migrator.js';export const pricingMigrations:readonly Migration[]=[{id:'020_pricing',sql:`CREATE TABLE pr_offers(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,name text NOT NULL,product_id text NULL REFERENCES cat_products(id),kind text NOT NULL CHECK(kind IN('percent','fixed')),value numeric(14,4) NOT NULL CHECK(value>0),min_qty numeric(14,3) NOT NULL DEFAULT 1 CHECK(min_qty>0),starts_at timestamptz NOT NULL,ends_at timestamptz NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),CHECK(ends_at IS NULL OR ends_at>starts_at));CREATE INDEX pr_offers_active_idx ON pr_offers(tenant_id,product_id,active,starts_at,ends_at);CREATE TABLE pr_price_updates(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES cat_products(id),old_price numeric(14,2) NOT NULL CHECK(old_price>=0),new_price numeric(14,2) NOT NULL CHECK(new_price>=0),source text NULL,note text NULL,status text NOT NULL CHECK(status IN('pending','applied','cancelled')),created_at timestamptz NOT NULL DEFAULT now(),applied_at timestamptz NULL);CREATE INDEX pr_price_updates_idx ON pr_price_updates(tenant_id,status,created_at DESC);`}];
