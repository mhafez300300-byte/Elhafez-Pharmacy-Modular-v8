import type{UnitOfWork}from'../../../core/db/unit-of-work.js';import{newId}from'../../../core/types/id.js';import type{AttendanceContract}from'../contracts/attendance-contract.js';import type{OrganizationContract}from'../../organization/contracts/organization-contract.js';import type{AuditContract}from'../../audit/contracts/audit-contract.js';import{AppError}from'../../../core/errors/app-error.js';
export class AttendanceService{constructor(private readonly uow:UnitOfWork,private readonly repo:AttendanceContract,private readonly org:OrganizationContract,private readonly audit:AuditContract){}async checkIn(t:string,u:string,branchId?:string,note?:string){return this.uow.withTransaction(async tx=>{const branch=branchId? (await this.org.listBranches(t)).find(b=>b.id===branchId&&b.active):await this.org.getDefaultBranch(t,tx);if(!branch)throw new AppError('BRANCH_REQUIRED','الفرع مطلوب',422);const v=await this.repo.checkIn({id:newId('att'),tenantId:t,userId:u,branchId:branch.id,note:note?.trim()||null},tx);await this.audit.record({tenantId:t,userId:u,action:'attendance.check_in',entity:'attendance',entityId:v.id,detail:{branchId:branch.id}},tx);return v;});}async checkOut(t:string,u:string,note?:string){return this.uow.withTransaction(async tx=>{const v=await this.repo.checkOut(t,u,note?.trim()||null,tx);await this.audit.record({tenantId:t,userId:u,action:'attendance.check_out',entity:'attendance',entityId:v.id,detail:{}},tx);return v;});}}
