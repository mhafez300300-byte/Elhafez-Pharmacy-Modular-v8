import type{Migration}from'../../../core/db/migrator.js';export const attendanceMigrations:readonly Migration[]=[{id:'024_attendance',sql:`CREATE TABLE attendance_sessions(id text PRIMARY KEY,tenant_id text NOT NULL REFERENCES org_tenants(id) ON DELETE CASCADE,user_id text NOT NULL REFERENCES id_users(id),branch_id text NOT NULL REFERENCES org_branches(id),check_in timestamptz NOT NULL DEFAULT now(),check_out timestamptz NULL,note text NULL);CREATE UNIQUE INDEX attendance_one_open_per_user ON attendance_sessions(tenant_id,user_id) WHERE check_out IS NULL;CREATE INDEX attendance_history_idx ON attendance_sessions(tenant_id,check_in DESC);`}];
