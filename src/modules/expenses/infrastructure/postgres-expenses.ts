import type{DbExecutor,DbTx}from'../../../core/db/types.js';import type{ExpenseContract,ExpenseView}from'../contracts/expense-contract.js';export class PostgresExpenseRepository implements ExpenseContract{constructor(private readonly db:DbExecutor){}async save(v:ExpenseView,tx:DbTx){await tx.query(`INSERT INTO exp_expenses(id,tenant_id,branch_id,user_id,category,description,amount,method,occurred_at) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9)`,[v.id,v.tenantId,v.branchId,v.userId,v.category,v.description,v.amount,v.method,v.occurredAt]);}async list(t:string,limit=200){const q=await this.db.query<any>(`SELECT id,tenant_id as "tenantId",branch_id as "branchId",user_id as "userId",category,description,amount::float,method,occurred_at::text as "occurredAt" FROM exp_expenses WHERE tenant_id=$1 ORDER BY occurred_at DESC LIMIT $2`,[t,Math.min(500,limit)]);return q.rows;}}
